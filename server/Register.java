package services;

/** Class that registers a new user */
public class Register {
   /** Default constructor */
   public Register() {}
   
   /** Registers a new user
    * @param request Request containing the information for the new user
    * @return A reponse containing an authentication token and person ID if succesful, and an error message if unsuccesful
    */
   public response.RegisterResponse register(request.RegisterRequest request) {return null;}
}